import React from 'react';
import LoadingState from './ui/LoadingState';

const PageLoader = () => (
    <LoadingState size="full" label="Loading tool…" description="Please wait while we prepare this tool." />
);

export default PageLoader;
